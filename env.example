# ── Optional seed wallet ──────────────────────────────────────────────────────
# You can leave this empty and add wallets from Telegram with:
# /addwallet <address>
WALLET_ADDRESS=

# Your own trading wallet to monitor for first buys and use for confirmed sells.
# This is required for trading-wallet detection; WALLET_ADDRESS is not used as a fallback.
TRADING_WALLET_ADDRESS=

# ── Solana RPC ─────────────────────────────────────────────────────────────────
# Free public endpoint — replace with Helius/QuickNode/Triton for production
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Optional websocket endpoint. If omitted, it is derived from SOLANA_RPC_URL.
# For Helius this is usually:
# SOLANA_WS_URL=wss://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY
SOLANA_WS_URL=wss://api.mainnet-beta.solana.com

# Minimum first-buy size in SOL before monitoring a new token.
# Set 0 to disable. Poll-only detections are skipped when this is enabled
# because the original buy size is unknown.
MIN_BUY_SOL=6

# ── GMGN API ───────────────────────────────────────────────────────────────────
# Get your key at https://gmgn.ai/ai  (apply for API access)
GMGN_API_KEY=YOUR_GMGN_API_KEY_HERE

# Optional second GMGN API key for multi-wallet Insider mode.
GMGN_API_KEY_2=

# Optional third GMGN API key — pre-buy axiom/empty single-buy scan (no tag, order-by buy_volume_cur, limit 50).
GMGN_API_KEY_3=

# GMGN signing key. Not used by the Jupiter Swap V2 sell flow.
GMGN_PRIVATE_KEY=

# GMGN API host. Routes are built as /v1/token/security and /v1/token/info.
# If GMGN gives you a dedicated host, set it here.
GMGN_API_BASE_URL=https://gmgn.ai

# direct = fetch GMGN HTTP routes directly
# cli    = use npx gmgn-cli token ... --raw
# auto   = try direct first, then fall back to CLI if blocked
GMGN_FETCH_MODE=cli

# ── PumpPortal Lightning trade execution ──────────────────────────────────────
# Create and fund a Lightning Wallet at PumpPortal, then set its public address
# as TRADING_WALLET_ADDRESS above. The API key controls that linked wallet.
PUMPPORTAL_API_KEY=
PUMPPORTAL_WALLET_ADDRESS=

# Bitquery OAuth token — Pump.fun creator count (follow-token "exactly 1 create" filter).
# Generate at https://account.bitquery.io/ — never commit the real token.
BITQUERY_ACCESS_TOKEN=ory_at_t_Lb_RIJLtj5JNscRmjWo_IzD8TPysNeYdszi8XAKno.ynqkuCABMRPOhwZ0iXI0QaYiX7xXdoE7WsaybaAEgBo

# Legacy local/Jupiter signing credentials. Lightning buys/sells do not use
# this private key, but it may still be used by older non-trade helpers.
JUPITER_PRIVATE_KEY=

# Jupiter Swap V2 requires x-api-key.
JUPITER_API_KEY=
JUPITER_SWAP_BASE_URL=https://api.jup.ag/swap/v2

# Helius API key for early bundler detection and monitoring.
HELIUS_API_KEY=

# Optional separate Helius key/RPC for wallets that receive transferred tokens
# from early bundlers. If RECEIVER_HELIUS_API_KEY is set and the RPC URLs below
# are blank, the bot derives Helius HTTP/WSS URLs from this key.
RECEIVER_HELIUS_API_KEY=

# Optional explicit receiver-wallet RPC/WSS overrides.
# RECEIVER_SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_SECOND_HELIUS_KEY
# RECEIVER_SOLANA_WS_URL=wss://mainnet.helius-rpc.com/?api-key=YOUR_SECOND_HELIUS_KEY

# Optional separate Helius key/RPC for creator-vault F1 program monitoring.
# If F1_HELIUS_API_KEY is set and the RPC URLs below are blank, the bot derives
# Helius HTTP/WSS URLs from this key.
F1_HELIUS_API_KEY=

# Optional explicit creator-vault F1 RPC/WSS overrides.
# F1_SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_F1_HELIUS_KEY
# F1_SOLANA_WS_URL=wss://mainnet.helius-rpc.com/?api-key=YOUR_F1_HELIUS_KEY

# Optional separate Helius key/RPC for Insider mode.
# If INSIDER_HELIUS_API_KEY is set and the RPC URLs below are blank, the bot
# derives Helius HTTP/WSS URLs from this key.
#
# IMPORTANT: INSIDER_HELIUS_API_KEY must be on a Helius Developer (or higher)
# plan. It is the ONLY key used to open Enhanced WebSocket
# (`transactionSubscribe`) connections everywhere in the codebase (feePayer
# sync, recipient watch, and insider/bundler wallet monitoring) — that
# Helius extension is Developer-plan-only, and none of the other
# INSIDER_HELIUS_API_KEY_2/3/4 keys are assumed to be on that plan. If this
# key is missing, every one of those sites silently falls back to the older
# onLogs + REST-fetch pattern instead (more Helius credits, slightly more
# latency, but functionally unchanged).
INSIDER_HELIUS_API_KEY=
INSIDER_HELIUS_API_KEY_2=

# Optional top-level feePayer *funder* wallet for the parallel funder-first flow.
# When set and funder-first mode is started, the bot watches this wallet 24/7 for
# SOL transfer-outs to potential feePayers (separate from follow-wallet backtrack).
INSIDER_FEEPAYER_FUNDER_ADDRESS=

# Auto-start discovery flows on boot (Telegram Start/Pause still works manually).
INSIDER_FOLLOW_WALLET_ENABLED=false
INSIDER_FUNDER_FIRST_ENABLED=false

# Follow-token: PumpPortal subscribeMigration + filter pipeline.
# Auto-start listener when true; token-level Telegram alerts also require this flag.
INSIDER_FOLLOW_TOKEN_ENABLED=true
# Max seconds between token CREATE and migrate (default 60).
INSIDER_FOLLOW_TOKEN_MAX_MIGRATION_AGE_SEC=60

INSIDER_BUNDLER_BUY_MIN_USD=100
INSIDER_BUNDLER_BUY_MAX_USD=200
# Insider sells required before buy gate (parallel with bundler GMGN scan)
INSIDER_REQUIRED_SELLS=5

# Optional explicit Insider mode RPC/WSS overrides.
# INSIDER_SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_INSIDER_HELIUS_KEY
# INSIDER_SOLANA_WS_URL=wss://mainnet.helius-rpc.com/?api-key=YOUR_INSIDER_HELIUS_KEY
# INSIDER_SOLANA_RPC_URL_2=https://mainnet.helius-rpc.com/?api-key=YOUR_SECOND_INSIDER_HELIUS_KEY
# INSIDER_SOLANA_WS_URL_2=wss://mainnet.helius-rpc.com/?api-key=YOUR_SECOND_INSIDER_HELIUS_KEY

# Default SOL amount for Insider mode buys. Can also be changed in Telegram.
INSIDER_BUY_SOL=0.025
# Mode-specific shared feePayer flow buy amounts. Defaults to INSIDER_BUY_SOL if omitted.
INSIDER_NORMAL_BUY_SOL=0.18
INSIDER_LOW_FUNDING_BUY_SOL=0.025

# Insider mode Market Cap thresholds (USD)
INSIDER_ENTRY_MC=50000
INSIDER_EXIT_MC=75000

# When filters fail, the bot sells using Jupiter Swap V2.
SELL_PERCENT=100

# Kept for compatibility. Jupiter Swap V2 uses its own slippage estimation.
SELL_AUTO_SLIPPAGE=false

# Kept for compatibility with older sell flows.
SELL_SLIPPAGE=50

# Priority fee for Jupiter Swap V2. 0.000012 SOL is about $0.001 at ~$83/SOL.
SELL_PRIORITY_FEE_SOL=0.000012

# Kept for compatibility with older sell flows.
SELL_ANTI_MEV=false

# ── Scheduler / Rate-limiter ───────────────────────────────────────────────────
# Target interval between consecutive GMGN polls for each token (ms)
MONITOR_INTERVAL=2000

# Legacy fallback setting. Linked trading-wallet tokens now stop at the
# active Apply filters at sample # decision instead of this time window.
MONITOR_WINDOW_MS=60000

# Minimum gap between any two GMGN HTTP requests (ms). Increase if you hit 429s.
RATE_LIMIT_MIN_TIME=500

# Max concurrent GMGN requests in flight at once
RATE_LIMIT_MAX_CONCURRENT=1

# ── Persistence ────────────────────────────────────────────────────────────────
DB_PATH=./data/monitor.db

# ── Logging ────────────────────────────────────────────────────────────────────
# debug | info | warn | error
LOG_LEVEL=info

# ── Telegram ──────────────────────────────────────────────────────────────────
# Create with @BotFather, then add it here.
TELEGRAM_BOT_TOKEN=

# Optional: chat id that receives automatic sample/summary cards.
# Commands work from any chat that messages the bot.
TELEGRAM_CHAT_ID=

# Fly.io / health check port
PORT=8080
